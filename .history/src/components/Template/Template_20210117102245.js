import React from 'react';

const Template = () => {
  return ( 
    <div className='template-container'>
      <h3>{new Date()}</h3>
      {stackHistory.map(() => {
        
      })}
    </div>
   );
}
 
export default Template;